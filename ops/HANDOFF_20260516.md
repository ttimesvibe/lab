# HANDOFF — 2026-05-16 세션 영역 이어가기 사료

다른 창에서 본 세션 영역 이어갈 때 박제. 본 사료가 단일 진실 — 코드 / commit / 문서 모두 정합 박제.

---

## 1. 본 세션 박제 영역 종합 (2026-05-15 ~ 16)

### lab repo (ttimesvibe/lab) commit 10 개

| commit | 의미 | 문서 박제 |
|---|---|---|
| `e459696` | L1493 await autoSaveToKV | POSTMORTEM #18 |
| `11e3235` | save PAYLOAD diffs/anal sizeHint (진단 — 제거됨) | (제거 박제) |
| `9e0b3b4` | load LOADED + tabFresh loaded sizeHint (진단 — 제거됨) | (제거 박제) |
| `e9d8332` | **_stableId 박제** ★ correction.diffs 손실 봉합 | POSTMORTEM #18 |
| `1868317` | onFileUpload review PUT | POSTMORTEM #18 |
| `fd38643` | **mount load review PUT** ★ 0차 검토 봉합 | POSTMORTEM #18 |
| `63b070f` | 본 세션 진단 log 제거 (prod cherry-pick 영역 정리) | POSTMORTEM #18 |
| `8526ce6` | POSTMORTEM + CHANGELOG #18 박제 | (자체) |
| `23301ae` | **highlight.clips last_write_wins** ★ 삭제 부활 봉합 | POSTMORTEM §10 + CHANGELOG #19 |
| `85c4e4d` | 드롭다운 라벨 "추가 삭제" → "자료" | CHANGELOG #20 |
| `9bde89f` | 자료 탭 재도입 (C_user 키 + _stableId) | CHANGELOG #20 |
| `e841be4` | 모델 매트릭스 prod 정합 롤백 (b369ccb 폐기) | CHANGELOG #21 |
| `a45d00c` | /correct 만 gpt-5.4 박제 | CHANGELOG #22 |

### prod editor repo (ttimesvibe/editor) commit 4 개

| commit | 의미 | 문서 박제 |
|---|---|---|
| `5ec7b28` | 5 fix cherry-pick (1차 교정 + 0차 검토) | CHANGELOG #18 |
| `2d9cbf1` | CHANGELOG #18 박제 | (자체) |
| `0084ac9` | highlight.clips last_write_wins cherry-pick | CHANGELOG #19 |
| `3051a7f` | 자료 탭 재도입 + 라벨 정합 cherry-pick | CHANGELOG #20 |

★ prod에 **미 cherry-pick 영역**:
- `a45d00c` /correct gpt-5.4 — lab 검증 후 prod 적용 의무 (사용자 명시 대기)

---

## 2. 본 세션의 진짜 본질 영역 (POSTMORTEM 박제)

### 본질 A — `_stableId` 박제 누락 (correction.diffs)
- worker `array_stable_id_union` 머지의 `idFn = _stableId ?? fallbackKeySync` 영역
- 클라/worker 어디에도 `_stableId` 박제 코드 X
- `/correct` 응답 chunk 구조에 fallback key 필드 (`blockIndex/posStart/posEnd/kind`) 없음
- → 모든 diffs 항목이 fallback key `"||||"` 동일 → 1개 압축
- **fix**: 클라 `ad.map(...)` 으로 _stableId 박제 (commit `e9d8332`)

### 본질 B — mount load 자동 0차 검토 생성 분기 PUT 누락
- App.jsx L766-797 분기 (correction 빈 + manuscript 있음 시 자동 setReviewData)
- 호출 시점 `isInitialLoad.current=true` → useEffect dirty skip → KV.review 영원히 빈
- 다음 mount stages filter (L716) 에서 review 영역 fetch X → 탭 비활성화 (닭-달걀)
- **fix**: setReviewData 직후 `await autoSaveToKV({reviewData})` 박제 (commit `fd38643`)

### 본질 C — `array_stable_id_union` 의 union 의미론 결함 (highlight.clips)
- PUT body에서 빠진 항목도 KV에 보존 → 사용자 삭제 인식 X
- 옛 앱 (ttimes-hilight) = 통째 PUT (last_write_wins). 통합 CMS 의미론 변경 시 trade-off 박제 누락
- 3 회 발견 영역 패턴 (9a6cef0 / 본 세션 #1 / 본 보고)
- **fix**: `worker/merge.js` `highlight.clips: { kind: "last_write_wins" }` (commit `23301ae`)

### 본질 D — dead 분기 audit 영역
- 옛 자료 탭 (C1 키) UI는 제거됐지만 처리 분기 2 곳 (App.jsx L1546, GuideTab L352) 잔존
- 사용자 명시로 dead 분기 재발견
- **fix**: C_user 키 신설 + C1 분기 유지 (옛 KV 정합 보존) (commit `9bde89f`)

---

## 3. 잠재 영역 (미 fix, 사용자 명시 원칙 정합)

| 영역 | 상태 | 발현 조건 |
|---|---|---|
| `L1714` 하이라이트 생성 영역 await 누락 | 미 fix | 사용자가 하이라이트 직후 페이지 종료 / 새 mount 영역 |
| `L1449` Step 0 metadata `.catch(() => {})` await 누락 | 미 fix | 사용자가 Step 0 직후 페이지 종료 |
| `modify.cards` array_stable_id_union | 미 fix | 사용자 cards 삭제 시 부활 위험 |
| `visualGuides` / `insertCuts` / `manualResources` array_stable_id_union | 미 fix | 같은 패턴, 사용자 삭제 시 부활 위험 |
| `guide.hl` array_stable_id_union | 미 fix | AI 생성이라 _stableId 박제됨, 사용자 삭제 영역 영역 제한 |
| 다른 AI 생성 항목의 _stableId 박제 | 미 fix | fallback key 필드가 실 데이터에 박혀 손실 X (비대칭 정합) |

★ 사용자 보고 시 같은 패턴 적용 의무 (★ 본 세션 fix 패턴 박제됨).

---

## 4. 운영 영역 박제

### KV PUT 한도 초과
- 본 세션 마지막 (2026-05-16 17:00 영역) 박제 — lab worker 모든 endpoint 500
- 원인: Cloudflare 무료 플랜 KV PUT 한도 (1,000/일) 초과
- 해결: 24h 대기 (UTC 00:00 / 한국 09:00 리셋)
- **prod 영향 0** (별 계정)
- 상세: POSTMORTEM §12

### 단어장 누적의 정상 부수
- Step 0 후 "AI 오인식 후보 0건" 박제 (사용자 보고)
- 원인: 본 세션 같은 docx 반복 박제 → 단어장 누적 (289+ 건) → AI가 거의 모든 후보 제외
- **결함 X, 정상 동작**. 다른 영상으로 검증 의무
- 상세: POSTMORTEM §12

---

## 5. 다음 세션 의무 영역

### 단기 (24h 후)
1. **lab 검증 재개** — KV 한도 리셋 후
   - /correct gpt-5.4 효과 박제 (1차 교정 품질 / 응답 시간 비교)
   - 새 docx 로 Step 0 오인식 후보 박제 박제 (단어장 누적 영향 검증)
2. **prod 적용 결정**
   - 만족 시: prod editor에 `a45d00c` cherry-pick (worker/index.js L2531 1 줄)
   - 명시 부탁드림

### 중기 (별 세션)
3. **dead 분기 audit** — 전체 코드베이스 grep
   - 본 세션의 C1 분기 외 영역 영역 영역
   - UI 제거 후 처리 분기 잔존 패턴 박제
4. **머지 의미론 audit** — array_stable_id_union 영역의 다른 항목
   - modify.cards / visualGuides / insertCuts / manualResources / guide.hl
   - 삭제 시나리오 테스트 추가 의무

### 장기 (선택)
5. **PUT 빈도 감소** — KV 한도 회피
   - 30s timer → 60s/120s
   - dedupe 강화 (snapshot 비교 박제됨, 추가 영역 가능)
6. **헌장 영역 확장**
   - "삭제 시 KV 정합" 명시
   - "dead code 금지" / "UI 제거 시 처리 분기도 같이 제거"
   - "단어장 누적 영역의 영역 영역" 박제 의무

---

## 6. 참조 사료

### lab repo
- `CHANGELOG.md` entry #18, #19, #20, #21, #22
- `ops/POSTMORTEM_DATA_LOSS_20260515.md` §1-§12
- `README.md` — 인프라 매트릭스
- `CLAUDE.md` 영역 — 운영 컨텍스트

### prod editor repo
- `CHANGELOG.md` entry #18, #19, #20
- `CLAUDE.md` — 계정 / KV / 배포 박제
- (prod에는 별 POSTMORTEM 박제 X — lab POSTMORTEM 참조)

### 빌드 / 배포 영역
- lab Worker: `lab.ttimes.workers.dev` (ttimesvibe 계정)
- lab Pages: `https://ttimesvibe.github.io/lab/` (build hash 변동)
- prod Worker: `alleditor.ttimes6000.workers.dev` (ttimes6000 계정)
- prod Pages: `https://ttimesvibe.github.io/editor/`

### 인증
- lab Auth Worker: `lab-auth.ttimes.workers.dev` (lab JWT_SECRET 영역 별 값)
- prod Auth Worker: `auth.ttimes6000.workers.dev`

---

## 7. 본 세션의 누적 학습 박제 (POSTMORTEM §6 참조)

### 추측 누적 → 사용자 명시 원칙 위반 패턴 (회피 의무)
1. **build hash 구분 의무** — 사용자 log의 build hash 매번 확인
2. **session id 구분 의무** — 콘솔 log의 여러 세션 혼재 박제
3. **호출 경로 추적 의무** — handleProcessFile vs onFileUpload vs handleFile vs mount load 자동 생성 분기
4. **빌드 직접 검증 의무** — fix 적용 후 빌드 산물에 실제 박제됐는지 grep 검증

### 사용자 직관 영역 우선 박제
- "1차 수정 끝나면 바로 저장 명령 하나 넣으면?" → L1493 await 발견
- "다른 영역 손실 X" (가설 반증) → _stableId 본질 발견
- "다른 작업 후 CMS 나갔다 들어옴" → mount load 자동 생성 분기 발견
- "8개 앱의 hilight에서는 문제 없는데" → array_stable_id_union 의미론 결함 발견
- "C1 코드가 남아있다는 건 스파게티" → dead 분기 audit 발견

★ 추측보다 사용자 시나리오 박제 우선 의무.

---

작성: 2026-05-16
최종 commit (lab): `a45d00c`
최종 commit (prod): `3051a7f`
